<ul>
	<% for (let i = messages.length - 1; i >= 0; i--) { %>
		<li>
			<h1><%= messages[i].user %></h1>
			<p><%= messages[i].text %></p>
			<span><%= messages[i].added %></span>
		</li>
	<% } %>
</ul>